# 簡報內容稿:儀表板發電量計算修正

> 用途:交給「Claude in PPT」依使用者既有母片生成投影片。共 2 頁,標題 + 要點 + 講者備註。格式/配色由母片決定,此稿只提供文字內容。

---

## 投影片 1 ── 問題:累計發電量嚴重偏低

**主標**:Bug 診斷 — 一整年只算到 268 度

**要點**
- 症狀:儀表板「累計能量」KPI 一整年只顯示約 268 度,但晴天單日全系統實際就發約 47 度,數字明顯不合理。
- 根本原因:KPI 靠加總資料表的 `daily_energy_Wh` 欄位,但這個欄位只有 2025-03 前 20 天有值——只佔全部資料的 0.6%,其餘 99.4% 是空白。
- 連鎖效應:後端把空白當 0,於是 391 天裡有 371 天的發電量完全沒被計入,總量被嚴重低估。
- 為何會這樣:舊的固定面板前處理流程會計算 `daily_energy_Wh`;後來改用 Z3A 雲端每 10 分鐘的資料併入後,沒有再補算這個欄位,導致欄位長期留空。

**對照數字**

| 項目 | 數值 |
|---|---|
| 有 `daily_energy_Wh` 的資料 | 5,206 / 935,316 筆(0.6%) |
| 有值的日期 | 僅 2025-03-01 ~ 03-20 共 20 天 |
| 修正前 KPI 顯示 | 約 268 度 |

**講者備註**:欄位本身沒壞,是「資料只填了前 20 天、計算又只認這個欄位」兩件事疊在一起,造成大幅低估。算法邏輯(每片每天取值再加總)其實沒錯,錯在來源欄位幾乎是空的。

---

## 投影片 2 ── 解法:改用功率積分(方案 B)

**主標**:新計算方式 — 用瞬時功率對時間積分

**要點**
- 核心公式:`能量 e_wh = 瞬時功率 power_W × 取樣間隔(小時)`,再依面板/日期/組合加總。`power_W` 欄位完整且正常(正午尖峰平均約 110 W)。
- 三條穩健規則:① 間隔用「與同面板前一筆的實際時間差」,上限 15 分鐘(避免設備離線的大缺口被當成持續發電而灌水);② 每片面板第一筆無前值 → 補 10 分鐘標準間隔;③ 重複時戳的時間差為 0 → 自動不重複計算。
- 實作範圍:在後端 `fixed_panel_api.py` 的資料載入函式統一算好 `e_wh`,三個端點共用——`kpi-summary`(總能量、排行)、`day-curve`(單日能量)、`panel-trend`(A/B 趨勢);沿用原欄名,前端與下游不需改動。
- 設計取捨:採「照實算」,資料缺口的日子(例如 2026-05-03~07 Z3A 採集中斷)維持 0、不做補值,確保數字可追溯。

**修正成果**

| 項目 | 修正前 | 修正後 |
|---|---|---|
| 累計能量(24 片固定面板) | 268 度 | **6,196 度** |
| 最佳組合 | 算不出 | **10°/180° = 635 度** |
| 最差組合 | 算不出 | 10°/160° = 427 度 |
| 最佳 vs 最差差異 | — | **48.8%** |

**講者備註**:方法在近一年區間先試算驗證過(全系統約 21 度/天、單片晴天 2~3 度,與預期一致)才正式寫入後端。最佳組合朝南/朝西、低傾角勝出,符合物理直覺,也是論文要呈現的研究結論。後續若要回補缺口天的資料,需在本機用 `z3a_collect.py` 抓取,且 Z3A 雲端歷史保留期有限。
